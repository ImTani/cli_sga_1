whoami:
- prints the username of the current user.
- the output shows i am logged in as root.
- this confirms that the active shell has administrator access.

id:
- displays user and group identities.
- shows my uid is 0 (root) and gid is 0 (root).
- shows i am also a member of the docker group.

echo $SHELL:
- prints the shell environment variable.
- shows the active command interpreter.
- the output is /bin/bash, which is the default for this session.

pwd:
- prints name of current/working directory.
- confirms my active path in the workspace.
- the output shows I am in /root/cli_sga_1.

ls -la:
- lists directory contents, permissions, and owners.
- includes hidden files and folders starting with a dot.
- shows that all five lab folders (q1-q5) are present.

ping -c 4 google.com:
- sends 4 icmp echo request packets to google.com.
- verifies network connectivity and dns resolution.
- the output shows 0% packet loss and a successful connection.

observations:
the system is running ubuntu under wsl2 as the root user. the default shell is bash, and we are working inside /root/cli_sga_1. network connectivity is functional and resolved google.com with zero packet loss.
